# apiVersion: v1
# kind: Service
# metadata:
#   name: {{ .Values.frontendProxy.appName }}
# spec:
#   selector:
#     app: {{ .Values.frontend.appName }}
#     tier: proxy
#   ports:
#     - protocol: TCP
#       port: {{ .Values.frontendProxy.port }}
#       targetPort: {{ .Values.frontendProxy.port }}
